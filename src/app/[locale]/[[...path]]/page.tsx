import type {Metadata} from "next";
import {Site} from "@/components/Site";
import {getVacancy,local,type Locale} from "@/data/site";
const map=(locale:Locale,path:string[])=>{const p=path.join("/");if(!p)return locale==="id"?"Karier yang Jelas. Lingkungan Kerja yang Terpercaya.":"A Clear Career Path. A Workplace You Can Trust.";if(p==="bali")return locale==="id"?"Cabang Bali — Karier dan Layanan":"Bali Branch — Careers and Services";if(p.includes("lowongan")||p.includes("careers")){const v=getVacancy(path.at(-1)||"");return v?local(v.title,locale):(locale==="id"?"Lowongan Bali":"Bali Careers")}const names:Record<string,string>={"tentang-kami":"Tentang Kami",about:"About Us",layanan:"Layanan",services:"Services",kontak:"Kontak",contact:"Contact","kebijakan-privasi":"Kebijakan Privasi","privacy-policy":"Privacy Policy","admin-preview":locale==="id"?"Pratinjau Admin":"Admin Preview"};return names[p]||"404"};
export async function generateMetadata({params}:{params:Promise<{locale:string;path?:string[]}>}):Promise<Metadata>{const {locale:raw,path=[]}=await params;const locale=raw as Locale;const title=map(locale,path);const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const current=`/${locale}${path.length?`/${path.join("/")}`:""}`;let other=path.join("/");if(locale==="id")other=other.replace(/^tentang-kami/,"about").replace(/^layanan/,"services").replace(/^kontak/,"contact").replace(/^kebijakan-privasi/,"privacy-policy").replace(/^bali\/lowongan/,"bali/careers").replace(/^bali\/lamar/,"bali/apply").replace(/\/berhasil$/,"/success");else other=other.replace(/^about/,"tentang-kami").replace(/^services/,"layanan").replace(/^contact/,"kontak").replace(/^privacy-policy/,"kebijakan-privasi").replace(/^bali\/careers/,"bali/lowongan").replace(/^bali\/apply/,"bali/lamar").replace(/\/success$/,"/berhasil");const otherLocale=locale==="id"?"en":"id";const v=getVacancy(path.at(-1)||"");const description=v?(locale==="id"?`Daftar sebagai ${local(v.title,locale)} di Bali. Proses mudah, tanpa biaya, dengan tindak lanjut WhatsApp.`:`Apply for ${local(v.title,locale)} in Bali through a simple, free process with WhatsApp follow-up.`):(locale==="id"?"Informasi karier, layanan keamanan, dan outsourcing PT Garda Karya Nusantara.":"Careers, security services, and outsourcing information from PT Garda Karya Nusantara.");return {title,description,alternates:{canonical:`${base}${current}`,languages:{[locale]:`${base}${current}`,[otherLocale]:`${base}/${otherLocale}${other?`/${other}`:""}`}},openGraph:{title,description,url:current},twitter:{title,description},robots:title==="404"?{index:false,follow:false}:undefined}}
export default async function Page({params}:{params:Promise<{locale:string;path?:string[]}>}){const {locale,path=[]}=await params;return <Site locale={locale as Locale} path={path}/>}
