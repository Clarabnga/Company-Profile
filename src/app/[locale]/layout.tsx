import type {Metadata} from "next";
import {NextIntlClientProvider} from "next-intl";
import {getMessages,setRequestLocale} from "next-intl/server";
import {notFound} from "next/navigation";
import "../globals.css";
export function generateStaticParams(){return [{locale:"id"},{locale:"en"}]}
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;const id=locale==="id";const base=new URL(process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000");const index=process.env.NEXT_PUBLIC_ALLOW_INDEXING==="true";return{metadataBase:base,title:{default:`${id?"Solusi Outsourcing Profesional":"Professional Outsourcing Solutions"} | PT Garda Karya Nusantara`,template:"%s | PT Garda Karya Nusantara"},description:id?"Layanan tenaga kerja terlatih dan terstruktur untuk mendukung keamanan serta efisiensi operasional perusahaan.":"Trained, well-managed personnel supporting secure and efficient business operations.",robots:{index,follow:index},alternates:{canonical:`/${locale}`,languages:{id:"/id",en:"/en","x-default":"/id"}},openGraph:{type:"website",siteName:"PT Garda Karya Nusantara",locale:id?"id_ID":"en_US",images:[{url:"/images/og.svg",width:1200,height:630,alt:"PT Garda Karya Nusantara"}]},twitter:{card:"summary_large_image"}}}
export default async function LocaleLayout({children,params}:{children:React.ReactNode;params:Promise<{locale:string}>}){const {locale}=await params;if(locale!=="id"&&locale!=="en")notFound();setRequestLocale(locale);const messages=await getMessages();return <html lang={locale}><body><NextIntlClientProvider messages={messages}>{children}</NextIntlClientProvider></body></html>}
